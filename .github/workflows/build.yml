name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Fix SDK and Plugin Configuration
        run: |
          # ভুল প্লাগইন ভার্সন সংশোধন
          grep -rl '9\.1\.1' . | xargs sed -i 's/9\.1\.1/8.7.3/g' || true
          
          # সঠিক compileSdk ও targetSdk নিশ্চিত করা
          find . -name "build.gradle.kts" -exec sed -i -E 's/compileSdk\(.*\)/compileSdk = 35/g' {} +
          find . -name "build.gradle.kts" -exec sed -i -E 's/targetSdk\(.*\)/targetSdk = 35/g' {} +
          find . -name "build.gradle.kts" -exec sed -i '/minorApiLevel/d' {} +
          
          # ক্যাশ বন্ধ রাখা
          echo "org.gradle.configuration-cache=false" >> gradle.properties

      - name: Setup Gradle 8.9 Wrapper
        run: |
          mkdir -p gradle/wrapper
          curl -sLo gradle/wrapper/gradle-wrapper.jar https://raw.githubusercontent.com/gradle/gradle/v8.9.0/gradle/wrapper/gradle-wrapper.jar
          cat << 'EOF' > gradle/wrapper/gradle-wrapper.properties
          distributionBase=GRADLE_USER_HOME
          distributionPath=wrapper/dists
          distributionUrl=https\://services.gradle.org/distributions/gradle-8.9-bin.zip
          networkTimeout=10000
          validateDistributionUrl=true
          zipStoreBase=GRADLE_USER_HOME
          zipStorePath=wrapper/dists
          EOF
          curl -sLo gradlew https://raw.githubusercontent.com/gradle/gradle/v8.9.0/gradlew
          chmod +x gradlew

      - name: Build Debug APK
        run: |
          ./gradlew assembleDebug --no-daemon --no-configuration-cache --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MySchoolApp-APK
          path: app/build/outputs/apk/debug/*.apk
